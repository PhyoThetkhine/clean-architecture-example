spring.application.name=clean-architecture-example

# MySQL datasource - update values below to match your environment
spring.datasource.url=jdbc:mysql://localhost:3306/books_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=pass
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Schema will be created by running the SQL in schema.sql (see README)
spring.sql.init.mode=always
